<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.mappers.purchaseMapper">

	<!-- 강의 구매 -->
	<insert id="purchaseCourse">
		insert into history_order_lecture (oli_no, m_no, payment)
		values (#{oli_no}, #{m_no}, #{payment})
	</insert>
	
</mapper>